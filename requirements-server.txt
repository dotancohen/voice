# Requirements for headless server deployments (sync server only)
# Does not include GUI (PySide6) or TUI (textual) dependencies

Flask>=3.0.0
Flask-CORS>=4.0.0
uuid6>=2024.1.12
diff-match-patch>=20230430
